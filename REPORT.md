# Conceptual Report - SecureNote

## 1. JS Engine vs Runtime
แม้ว่าเราจะเขียนแอปพลิเคชัน SecureNote ด้วย JavaScript (หรือ TypeScript) ทั้งโปรเจกต์ แต่โค้ดของเราถูกประมวลผลในสภาพแวดล้อมที่ต่างกันอย่างสิ้นเชิงระหว่าง Frontend และ Backend:

* **JavaScript Engine:** คือโปรแกรมที่ทำหน้าที่คอมไพล์และรันโค้ด JS ให้เป็นภาษาเครื่อง (Machine Code) ทั้ง Node.js (Backend) และเบราว์เซอร์อย่าง Google Chrome (Frontend) ต่างก็ใช้ **V8 Engine** เป็นตัวขับเคลื่อนหลักเหมือนกัน (หากผู้ใช้ใช้ Firefox จะเป็น SpiderMonkey หรือ Safari จะเป็น JavaScriptCore)
* **Runtime Environment (สภาพแวดล้อมการทำงาน):** นี่คือจุดที่ทำให้ Frontend และ Backend แตกต่างกัน
    * **Frontend (Browser Runtime):** โค้ดของหน้าเว็บถูกส่งไปรันที่เครื่องของผู้ใช้งาน (Client) ในเบราว์เซอร์ ซึ่ง Runtime นี้จะมี Web APIs ให้ใช้ เช่น `window`, `document` (สำหรับการจัดการ DOM), `fetch` API และ Local Storage แต่จะ**ไม่มีสิทธิ์**เข้าถึงไฟล์ในเครื่องผู้ใช้หรือฐานข้อมูลโดยตรงเพื่อความปลอดภัย
    * **Backend (Node.js Runtime):** โค้ดถูกรันบนเซิร์ฟเวอร์ (เช่น บน Render) Node.js นำ V8 Engine มาครอบด้วย C++ และตัด Web APIs ทิ้งไป แต่เพิ่มไลบรารีของตัวเองเข้ามาแทน ทำให้สามารถทำสิ่งที่เบราว์เซอร์ทำไม่ได้ เช่น การอ่าน/เขียนไฟล์ (`fs`), การจัดการเน็ตเวิร์ก (`http`), และการเชื่อมต่อกับ MongoDB

## 2. DOM (Document Object Model)
DOM คือโครงสร้างข้อมูลแบบต้นไม้ (Tree) ที่เบราว์เซอร์สร้างขึ้นเพื่อใช้อ่านและแสดงผลหน้า HTML เนื่องจากโปรเจกต์ SecureNote ใช้ **React (ผ่าน Next.js)** การอัปเดตหน้าจอจึงไม่ได้เข้าไปจัดการกับ Real DOM ตรงๆ (เพราะการแก้ Real DOM บ่อยๆ ทำให้แอปพลิเคชันทำงานช้า) แต่จะใช้แนวคิดที่เรียกว่า **Virtual DOM**

**การทำงานของ Virtual DOM ใน SecureNote:**
1.  เมื่อคุณพิมพ์ข้อความลงในช่องสร้างโน้ต หรือได้รับข้อมูลใหม่จาก API React จะสร้างจำลองโครงสร้างหน้าจอขึ้นมาในหน่วยความจำเรียกว่า Virtual DOM
2.  เมื่อมีการเปลี่ยนแปลง State (เช่น กดปุ่ม Save) React จะสร้าง Virtual DOM ต้นใหม่ขึ้นมา
3.  **Diffing Algorithm:** React จะนำ Virtual DOM ต้นใหม่ไปเปรียบเทียบกับต้นเก่า เพื่อหาว่ามี "จุดไหนบ้างที่เปลี่ยนแปลงไป" (เช่น มีแค่กล่องข้อความ 1 กล่องที่ถูกเพิ่มเข้ามา)
4.  **Reconciliation:** จากนั้น React จะนำเฉพาะการเปลี่ยนแปลงนั้นๆ ไปอัปเดตที่ Real DOM บนเบราว์เซอร์จริงๆ วิธีนี้ทำให้หน้าจอของ SecureNote อัปเดตได้ลื่นไหลและใช้ทรัพยากรน้อยที่สุด

## 3. HTTP / HTTPS
เมื่อผู้ใช้งานกรอกข้อมูลโน้ตเสร็จแล้วกดปุ่ม "Submit" จะเกิดวงจร **Request/Response Cycle** ระหว่าง Frontend และ Backend ดังนี้:

* **Request (คำร้องขอ):** Frontend จะส่ง HTTP POST Request ไปยัง Backend (เช่น `POST /notes`) 
    * **Headers:** ข้อมูลที่แนบไปส่วนหัว เช่น
        * `Content-Type: application/json` (บอก Backend ว่าข้อมูลที่ส่งไปเป็นรูปแบบ JSON)
        * `Authorization: Bearer <token>` (หากระบบมีการล็อกอิน จะส่ง Token เพื่อยืนยันตัวตน)
        * `Origin` และ `User-Agent` (บอกที่มาและประเภทของเบราว์เซอร์)
    * **Body:** ข้อมูลโน้ตจริงๆ (เช่น `{ title: "My Note", content: "Hello" }`)
* **Response (การตอบกลับ):** เมื่อ Backend ตรวจสอบข้อมูลและบันทึกลง MongoDB เสร็จสิ้น จะส่ง HTTP Status Code กลับมา (เช่น `201 Created`) พร้อมกับข้อมูลโน้ตที่ถูกบันทึกแล้ว เพื่อให้ Frontend นำไปแสดงผล

**ทำไม HTTPS ถึงสำคัญสำหรับ Production?**
เวลาที่คุณรันโค้ดในเครื่องตัวเอง (Localhost) การใช้ HTTP นั้นไม่มีปัญหา แต่เมื่อนำขึ้นระบบจริง (เช่น บน Vercel/Render) **HTTPS (Hypertext Transfer Protocol Secure)** เป็นสิ่งจำเป็นเด็ดขาด เพราะ HTTPS จะทำการ **เข้ารหัสข้อมูล (Encryption)** ผ่านโปรโตคอล TLS/SSL หากคุณใช้แค่ HTTP ข้อมูลที่วิ่งผ่านอินเทอร์เน็ตจะเป็น Text ธรรมดา แฮกเกอร์ที่ดักจับสัญญาณอยู่ระหว่างทาง (Man-in-the-Middle) จะสามารถอ่านเนื้อหาโน้ตทั้งหมด หรือแม้แต่ขโมย Token ของผู้ใช้งานไปได้เลย

## 4. Environment Variables
Environment Variables หรือไฟล์ `.env` คือที่สำหรับเก็บ "ความลับ" และ "การตั้งค่าตามสภาพแวดล้อม" ของแอปพลิเคชัน

**ทำไมเราถึงเก็บ SECRET_TOKEN ไว้ในไฟล์ .env ของ Backend?**
`SECRET_TOKEN` มักใช้สำหรับการเซ็นลายเซ็น (Sign) ให้กับ JWT (JSON Web Token) หรือเข้ารหัสข้อมูลสำคัญ โค้ดในฝั่ง Backend จะถูกรันอยู่บนเซิร์ฟเวอร์อย่างมิดชิด ผู้ใช้งานทั่วไปไม่สามารถเข้ามาดูซอร์สโค้ดของ Backend ได้ การเก็บความลับไว้ที่นี่จึงปลอดภัย (ไฟล์ `.env` จะไม่ถูก Push ขึ้น GitHub ด้วย)

**จะเกิดอะไรขึ้นถ้าเราเอาความลับไปไว้ในโค้ดฝั่ง Frontend?**
หากคุณเผลอนำ `SECRET_TOKEN` หรือ `MONGO_URI` ไปผูกไว้ในฝั่ง Frontend (เช่น ในไฟล์ของ Next.js โดยไม่ระวัง) ตัวแปรเหล่านั้นจะถูกมัดรวม (Bundled) ไปกับไฟล์ JavaScript ที่ส่งไปยังเบราว์เซอร์ของผู้ใช้ ทุกคนที่เปิดเว็บ SecureNote สามารถกด **F12 (Developer Tools)** เข้าไปดูที่แท็บ Sources/Network และ **อ่านรหัสผ่านหรือ Token ของคุณได้ทั้งหมด** ซึ่งจะนำไปสู่การเจาะฐานข้อมูล หรือการปลอมแปลงตัวตนเป็นแอดมินระบบได้ทันทีครับ

--- 